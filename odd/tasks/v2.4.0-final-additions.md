# Feature: v2.4.0 final additions

Repo: davalillo/mql-language-server · Branch: (per task) · Base: main @ dc7588a

## Goal

Close three items before sealing v2.4.0 stable:
1. nuget.org install: document the real install command + end-to-end smoke test of
   `dotnet tool install -g mql-language-server --prerelease` from nuget.org.
2. didSave: `DidSaveTextDocumentHandler` was never registered in Program.cs (residual
   finding from token-backed-references verify). Decide wire vs remove; implement.
3. CI vulnerability gate: `dotnet list package --vulnerable --include-transitive` step
   failing the build in build.yml.

## Tasks

- [x] T1. Explore didSave: the handler class does not even exist today; server uses pull
      diagnostics (DiagnosticHandler = IDocumentDiagnosticHandler). Decision: do not wire —
      by design. Documented with a comment at the sync-handler registrations in Program.cs.
- [x] T2. Implemented as T1 decision (comment-only, commit 7f8be2a).
- [x] T3. CI vulnerability gate: `dotnet list MqlLanguageServer.sln package --vulnerable
      --include-transitive` added to ci.yml `build-and-test` after the Test step (the job
      lives in ci.yml, not build.yml) — commit 6586597.
- [x] T4. LOCAL_INSTALLATION.md: Método 1 = dotnet tool install from nuget.org
      (install/update/uninstall, --prerelease for rc), renumbered local methods, header/footer
      de-framed — commit 4db83dc.
- [x] T5. Smoke test (gentle-ai-verify): PASS — tool installs from nuget.org (2.4.0-rc.3),
      binary runs, LSP initialize handshake returns capabilities + serverInfo; uninstalled
      afterwards. Note: displayed version is 2.4.0 by design (ServerVersion.cs drops
      prerelease suffix); DOTNET_ROOT failure is a local Nix artifact, not a package defect.
- [x] T6. Work-unit commits: 6586597 (ci), 4db83dc (docs), 7f8be2a (docs/server).

## Outcome

All three pre-seal items closed. Ready to seal v2.4.0 stable (CHANGELOG + csproj Version
2.4.0.0 + tag) as the next step.

## Notes

- Icon polish explicitly excluded (user decision).
- After T1-T5: seal 2.4.0 stable (separate release flow).
